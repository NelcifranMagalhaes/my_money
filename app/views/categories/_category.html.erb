
<div class="card text-center" id="<%= dom_id category %>">
  <div class="card-header">
    <%= category.name %>
  </div>
  <div class="card-body">
    <p class="card-text"><%= category.description %></p>
  </div>
</div>